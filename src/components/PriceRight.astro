---
import Copyright from "./Copyright.astro";
import PricesSign from "../assets/icons/sign/prices-sign.svg";
import Subtle from "./Subtle.astro";
import { socialLinks } from "../social-links";
import ContactButton from "./ContactButton.astro";
import SvgAnimation from "./SvgAnimation.astro";

const { className } = Astro.props;
---

<div class:list={[className, "section-column__wrapper"]}>
  <div class="section-column__wrapper-inner">
    <h2 class="section__title">
      Гибкие пакеты и <span class="brand-color italic underline"
        >индивидуальный

        <SvgAnimation>
          <PricesSign />
        </SvgAnimation>
      </span> подход <br />к каждой паре
    </h2>
    <div class="content">
      <p class="content__title">
        От первого знакомства до последнего бокала шампанского в финале вечера —
        мы берём на себя весь процесс: <br /> от идеи до реализации.
      </p>
      <div class="content__list">
        <div class="content__column">
          <span>Концепция и <br /> визуальное оформление</span>

          <span> Подбор команды <br /> (ведущий, фотограф, и др.)</span>
          <span>Поиск и бронирование <br />площадки</span>
        </div>
        <div class="content__column">
          <span>Ведение переговоров <br /> с подрядчиками</span>
          <span>Сметная документация <br /> и финконтроль</span>
          <span>Приятные бонусы <br /> от агенства</span>
        </div>
      </div>
      <p class="content__budget">
        Стоимость услуг агентства составляет <br /> 10% от бюджета мероприятия.
      </p>

      <p class="content__budget">
        Фиксированные тарифы обсуждаются <br /> в процессе подготовки.
      </p>

      <div class="contact-button">
        <ContactButton
          link={socialLinks.wu}
          class="content__contact"
          backgroundColor="240, 216, 181"
          alpha="1"
          textColor="rgba(85, 85, 85, 1)"
        >
          Рассчитать стоимость</ContactButton
        >
      </div>
    </div>
  </div>
  <Copyright />
</div>

<style>
  .content {
    color: var(--text-color-secondary);
    margin-top: 4.9rem;
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
  }
  .content__title {
    margin-bottom: 2rem;
    text-align: center;
    font-size: var(--fs-14);
    line-height: var(--lh-14);
    max-width: 25.8125rem;
  }
  .content__list {
    display: flex;
    margin-bottom: 2rem;
    justify-content: space-evenly;
    gap: 1rem;
    width: 100%;
  }
  .content__column {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }
  .content__budget {
    text-align: center;
    margin-bottom: 1rem;
    margin-left: auto;
    margin-right: auto;
    font-size: var(--fs-14);
    line-height: var(--lh-14);
  }

  .content__column > span {
    flex: 1;
  }

  .contact-button {
    justify-content: center;
    display: flex;
    margin-bottom: 1rem;
  }
  .content__column {
    font-size: var(--fs-14);
    line-height: var(--lh-14);
  }
  @media (max-width: 455px) {
    /* .section__title {
      font-size: var(--fs-42);
      line-height: var(--lh-42);
      letter-spacing: 0;
      font-weight: 400;
    } */
  }
</style>
