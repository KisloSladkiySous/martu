---
import Underline from "../assets/icons/sign/hover-underline.svg";
import FirstSign from "../assets/icons/sign/review-sign-1.svg";
import SecondSign from "../assets/icons/sign/review-sign-2.svg";
import IconButton from "./IconButton.astro";
import Subtle from "./Subtle.astro";
import SvgAnimation from "./SvgAnimation.astro";
import Yandex from "../assets/icons/yandex.svg";
import Instagramm from "../assets/icons/inst.svg";
import Vk from "../assets/icons/vk.svg";
import { socialLinks } from "../social-links";
const { className } = Astro.props;
---

<div class:list={[className, "section-column__wrapper"]}>
  <Subtle lines={["Отзывы тех, кто доверил нам самое важное"]} delay={400} />
  <h2 class="section__title">
    То, что <span class="brand-color italic underline"
      >невозможно

      <SvgAnimation>
        <FirstSign />
      </SvgAnimation>
    </span>
    <br />
    <span class="brand-color italic underline"
      >придумать
      <SvgAnimation delay={0.6}>
        <SecondSign />
      </SvgAnimation>
    </span> — только прожить
  </h2>
  <div class="review-description">
    <p class="text">
      И когда спустя месяцы или даже годы нам пишут: «Мы до сих пор вспоминаем
      тот день, как самый счастливый» — это больше, чем любая награда.
    </p>
    <p class="text">
      Мы знаем, что эмоции не подделаешь, что тепло не рисуется в фотошопе и что
      искренность нельзя прописать в ТЗ. Поэтому всё, что вы прочтёте —
      настоящее, написанное по зову сердца. Именно такие истории вдохновляют нас
      снова и снова вкладывать душу в каждую деталь.
    </p>
  </div>
</div>

<div class="media">
  <p class="media__title">Больше отзывов:</p>
  <div class="media__links">
    <IconButton areaLabel="отзывы Яндекс" link={socialLinks.vk}>
      <Yandex />

      <IconButton areaLabel="отзывы Инстаграмм" link={socialLinks.inst}>
        <Instagramm />

        <IconButton areaLabel="отзывы Вконтакте" link={socialLinks.vk}>
          <Vk />
        </IconButton>
      </IconButton>
      <style is:global>
        .review-description {
          margin-top: 4rem;
          display: flex;
          flex-direction: column;
          text-align: center;
          gap: 2rem;
          color: var(--text-color-secondary);
          justify-content: center;
          align-items: center;
        }
        .text {
          max-width: 23.125rem;
        }
        .media {
          margin-top: auto;
          display: flex;
          flex-direction: column;
          text-align: center;
          gap: 1rem;
          margin-bottom: 1rem;
        }
        .media__title {
          color: var(--text-color-fourth);
          margin-bottom: 1rem;
        }
        .media__links {
          color: var(--brand-color);
          display: flex;
          gap: 3rem;
          justify-content: center;
        }
        @media (max-width: 455px) {
          .section__title {
            font-size: var(--fs-42);
            line-height: var(--lh-42);
            letter-spacing: 0;
            font-weight: 400;
          }
          .review-description {
            font-size: var(--fs-14);
            line-height: var(--lh-14);
          }
        }
      </style>
    </IconButton>
  </div>
</div>
