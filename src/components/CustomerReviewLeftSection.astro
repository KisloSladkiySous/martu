---
import Underline from "../assets/icons/sign/hover-underline.svg";
import FirstSign from "../assets/icons/sign/review-sign-1.svg";
import SecondSign from "../assets/icons/sign/review-sign-2.svg";
import IconButton from "./IconButton.astro";
import Subtle from "./Subtle.astro";
import SvgAnimation from "./SvgAnimation.astro";
import Yandex from "../assets/icons/yandex.svg";
import Instagramm from "../assets/icons/inst.svg";
import Vk from "../assets/icons/vk.svg";
import { socialLinks } from "../social-links";
const { className } = Astro.props;
---

<div class:list={[className, "section-column__wrapper"]}>
  <div class="section-column__wrapper-inner">
    <div class="center-container">
      <Subtle
        lines={["Отзывы тех, кто доверил", "нам самое важное"]}
        delay={400}
      />
      <h2 class="section__title">
        То, что <span class="brand-color italic underline"
          >невозможно

          <SvgAnimation bottom="0.3rem">
            <FirstSign />
          </SvgAnimation>
        </span>
        <br />
        <span class="brand-color italic underline"
          >придумать
          <SvgAnimation delay={0.6}>
            <SecondSign />
          </SvgAnimation>
        </span> — только прожить
      </h2>
      <div class="review-description">
        <p class="text">
          И когда спустя месяцы или даже годы нам пишут: «Мы до сих пор
          вспоминаем тот день, как самый счастливый» — это больше, чем любая
          награда.
        </p>
        <p class="text">
          Мы знаем, что эмоции не подделаешь, что тепло не рисуется в фотошопе и
          что искренность нельзя прописать в ТЗ. Поэтому всё, что вы прочтёте —
          настоящее, написанное по зову сердца. Именно такие истории вдохновляют
          нас снова и снова вкладывать душу в каждую деталь.
        </p>
      </div>
    </div>

    <div class="media">
      <p class="media__title">Больше отзывов:</p>
      <div class="media__links">
        <IconButton areaLabel="отзывы Яндекс" link={socialLinks.vk}>
          <Yandex />
        </IconButton>
        <IconButton areaLabel="отзывы Инстаграмм" link={socialLinks.inst}>
          <Instagramm />
        </IconButton>
        <IconButton areaLabel="отзывы Вконтакте" link={socialLinks.vk}>
          <Vk />
        </IconButton>
      </div>
    </div>
  </div>
</div>
<style>
  .center-container {
    display: flex;
    height: 100%;
    justify-content: center;
    flex-direction: column;
  }
  .review-description {
    margin-top: clamp(1.5rem, 3vw, 4rem);
    margin-left: auto;
    margin-right: auto;
    display: flex;
    flex-direction: column;
    text-align: center;
    gap: 2rem;
    color: var(--text-color-secondary);
    justify-content: center;
    align-items: center;
    max-width: 22.375rem;
    margin-bottom: 1rem;
  }

  .media {
    margin-bottom: 1rem;
    margin-left: auto;
    margin-right: auto;
    max-width: 23.125rem;
  }
  .media__title {
    font-size: var(--fs-14);
    line-height: var(--lh-14);
    color: var(--text-color-fourth);
    text-align: center;
    margin-bottom: 1rem;
  }
  .media__links {
    display: flex;
    justify-content: center;
    gap: 1rem;
  }

  @media (max-width: 455px) {
    .media__links {
      gap: 0.5625rem;
    }
    .review-description {
      font-size: var(--fs-14);
      line-height: var(--lh-14);
    }
  }

  @media (max-width: 375px) {
    .review-description {
      gap: 1.5rem;
    }
  }
</style>
