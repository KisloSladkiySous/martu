---
import PortfolioCase from "../components/PortfolioCase.astro";
import Layout from "../layouts/Layout.astro";
import ScrollToTopButton from "../components/ScrollToTopButton.astro";
import fetchApi from "../lib/strapi";

// const cases = await fetchApi<any[]>({
//   endpoint: "cases?populate=*", // the content type to fetch
//   wrappedByKey: "data", // the key to unwrap the response
// });
// console.log(cases);
---

<Layout title="Martu - Портфолио">
  <ScrollToTopButton />
  <!-- <div class="cases">
    {
      cases.map((caseItem) => (
        <PortfolioCase
          folder={caseItem.slug}
          year={caseItem.date}
          names={caseItem.names}
          length={caseItem.images.length || 24}
          images={caseItem.images}
        />
      ))
    }
  </div> -->
</Layout>
<style>
  .cases {
    display: flex;
    flex-direction: column;
    padding: 13.25rem 10.25rem;
    gap: 2rem;
  }
  @media (max-width: 1025px) {
    .cases {
      display: flex;
      flex-direction: column;
      padding: 5rem 1.25rem;
    }
  }
</style>
